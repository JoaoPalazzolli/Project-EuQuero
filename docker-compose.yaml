version: '3.8'

services:
  mysql:
    image: mysql:latest
    container_name: mysql-euquero
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: admin
      MYSQL_DATABASE: euquero
    ports:
      - "3307:3306"
    volumes:
      - ./mysql-data:/var/lib/mysql
